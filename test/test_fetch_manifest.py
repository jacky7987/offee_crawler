from fetch_manifest import fetch_all_pages

'''
def test_fetch_all_pages():
    xml_site = 'https://www.bargain-cafe.com/sitemap.xml'
    brand_name = 'bargain'

    result = fetch_all_pages(xml_site, brand_name, save_html=True)

    #print(result)

    assert len(result)>0
'''